Generic implementations of functionality for embedded platforms that
typically only have a limited C-library and especially no (or limited)
support for dynamic memory management (malloc()).

This folder intentionally does not contain a CMakeLists.txt since its
files are intented to be included by the correspondig platforms.
